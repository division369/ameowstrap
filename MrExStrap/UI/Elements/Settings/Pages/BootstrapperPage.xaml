<ui:UiPage x:Class="ExploitStrap.UI.Elements.Settings.Pages.BehaviourPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:ExploitStrap.UI.Elements.Settings.Pages"
      xmlns:controls="clr-namespace:ExploitStrap.UI.Elements.Controls"
      xmlns:models="clr-namespace:ExploitStrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:ExploitStrap.Resources"
      d:DataContext="{d:DesignInstance Type=models:BehaviourViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="BehaviourPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock Margin="0,0,0,8" Text="{x:Static resources:Strings.Menu_Behaviour_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

        <controls:OptionControl
            Header="{x:Static resources:Strings.Menu_Behaviour_ConfirmLaunches_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_ConfirmLaunches_Description}">
            <ui:ToggleSwitch IsChecked="{Binding ConfirmLaunches, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="Prompt for version when launching"
            Description="When on, every Roblox launch pops a small picker so you can choose a Versions Manager profile right before the bootstrapper starts — no trip back into the settings menu. Comes after the VIP server picker when that's enabled. Picker is skipped on quiet/silent launches and on Studio.">
            <ui:ToggleSwitch IsChecked="{Binding ShowVersionPickerOnLaunch, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="Join the emptiest server on launch"
            Description="When on, a normal game launch drops you into the least-populated public server of that game instead of letting Roblox pick. Adds a moment to launch while it finds the server. Only applies to normal game launches — friend joins, Server Browser joins, VIP servers and re-joins are left alone, and it falls back to a normal launch if it can't find one.">
            <ui:ToggleSwitch IsChecked="{Binding JoinEmptiestServerOnLaunch, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl
            Header="Confirm non-LIVE launches"
            Description="When on (default), launching Roblox via a profile pinned to an older build pops a yes/no confirmation first. LIVE-channel launches never prompt. Turn this off if you're constantly switching to downgraded builds and don't want the extra click.">
            <ui:ToggleSwitch IsChecked="{Binding ConfirmNonLiveLaunch, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_BackgroundUpdates_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_BackgroundUpdates_Description}">
            <ui:ToggleSwitch IsChecked="{Binding BackgroundUpdates, Mode=TwoWay}" />
        </controls:OptionControl>

        <controls:OptionControl 
            Header="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Title}"
            Description="{x:Static resources:Strings.Menu_Behaviour_ForceRobloxReinstall_Description}">
            <controls:OptionControl.Style>
                <Style TargetType="controls:OptionControl">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsRobloxInstallationMissing, Mode=OneTime}" Value="True">
                            <Setter Property="IsEnabled" Value="False" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:OptionControl.Style>
            <ui:ToggleSwitch IsChecked="{Binding ForceRobloxReinstallation, Mode=TwoWay}" />
        </controls:OptionControl>
    </StackPanel>
</ui:UiPage>
